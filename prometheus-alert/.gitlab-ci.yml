
# cache:
#   paths:
#       - ${CI_PROJECT_DIR}/.m2/repository

variables:
   VERSION: 1.0.${CI_PIPELINE_ID}
  #  MAVEN_REPO_PATH: ${CI_PROJECT_DIR}/.m2/repository
  #  JAVA_OPTS: -XX:MaxRAMPercentage=90 

stages:
  #  - build-backend
  #  - release-backend
  #  - vault
   - deploy-prometheus

# build:
#    stage: build-backend
#    image:
#       name: gcr.io/kaniko-project/executor:v1.9.0-debug
#       entrypoint: [""]
#    script:
#       - echo "test"
#       - /kaniko/executor
#          --context "${CI_PROJECT_DIR}/backend"
#          --dockerfile "${CI_PROJECT_DIR}/backend/Dockerfile"
#          --destination "${CI_REGISTRY_IMAGE}/sausage-backend:$CI_COMMIT_SHA"
#          --build-arg VERSION=$VERSION
#          --cache=true
#    rules:
#       - if: '$CI_COMMIT_MESSAGE =~ /backend/i'
#       - if: '$CI_COMMIT_MESSAGE =~ /all/i'
#       - changes:
#          - backend/**/*

# release:
#    variables:
#       GIT_STRATEGY: none
#    image:
#       name: gcr.io/go-containerregistry/crane:debug
#       entrypoint: [ "" ]
#    cache: [ ]
#    stage: release-backend
#    before_script:
#       - crane auth login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
#    script:
#       - crane tag $CI_REGISTRY_IMAGE/sausage-backend:$CI_COMMIT_SHA $VERSION
#    rules:
#       - if: '$CI_COMMIT_MESSAGE =~ /backend/i'
#       - if: '$CI_COMMIT_MESSAGE =~ /all/i'
#       - changes:
#          - backend/**/*

# vault:
#    stage: vault
#    image: vault:1.11.3
#    before_script:
#       - apk add openssh-client bash
#       - eval $(ssh-agent -s)
#       - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
#       - mkdir -p ~/.ssh
#       - chmod 700 ~/.ssh
#       - echo "$SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
#       - chmod 644 ~/.ssh/known_hosts
#    script:
#       - export VAULT_TOKEN="$(vault write -field=token auth/jwt/login role=sausage-store jwt=$CI_JOB_JWT)"
#       - echo "SPRING_DATASOURCE_URL=$(vault kv get -field=spring.datasource.url secret/sausage-store)" > deploy.env
#       - echo "SPRING_DATASOURCE_USERNAME=$(vault kv get -field=spring.datasource.username secret/sausage-store)" >> deploy.env
#       - echo "SPRING_DATASOURCE_PASSWORD=$(vault kv get -field=spring.datasource.password secret/sausage-store)" >> deploy.env
#       - echo "SPRING_DATA_MONGODB_URI=$(vault kv get -field=spring.data.mongodb.uri secret/sausage-store)" >> deploy.env
#       - ls -l deploy.env 
#    artifacts:
#       paths:
#          - deploy.env
#    environment:
#       name: production/backend
#    rules:
#       - if: '$CI_COMMIT_MESSAGE =~ /backend/i'
#       - if: '$CI_COMMIT_MESSAGE =~ /all/i'
#       - changes:
#          - backend/**/*

deploy:
   stage: deploy-prometheus
   image: docker:24.0.7-alpine3.19
   environment:
      name: production/prometheus
   before_script:
      - apk add docker-cli-compose openssh-client
      - eval $(ssh-agent -s)
      - echo "$SSH_PRIVATE_KEY"| tr -d '\r' | ssh-add -
      - mkdir -p ~/.ssh
      - chmod 600 ~/.ssh
      - echo "$SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
      - chmod 644 ~/.ssh/known_hosts
      - docker context create remote --description "remote ssh" --docker "host=ssh://${DEV_USER}@${DEV_HOST}"
   script:
      - ssh ${DEV_USER}@${DEV_HOST} "docker network ls"
      # - ssh ${DEV_USER}@${DEV_HOST} "docker network create -d bridge sausage_network || true"
      # - BLUE=$(ssh ${DEV_USER}@${DEV_HOST} 'docker ps -a | grep backend-blue | wc -l')
      # - if [ $BLUE -gt 0 ]; then DEPLOY="backend-green"; REMOVE="backend-blue"; else DEPLOY="backend-blue"; REMOVE="backend-green"; fi
      # - ssh ${DEV_USER}@${DEV_HOST} "docker rm -f \$(docker ps -aq --filter name=$DEPLOY) || true"  
      # - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
      # - docker --context remote compose --env-file deploy.env up $DEPLOY -d --pull always --force-recreate;
      # - ssh ${DEV_USER}@${DEV_HOST} "until docker ps --filter health=healthy | grep -q $DEPLOY; do sleep 1; echo Not healthy backend yet;done;echo Backend is healthy!"
      # - ssh ${DEV_USER}@${DEV_HOST} "docker rm -f \$(docker ps -aq --filter name=$REMOVE) || true; docker rmi -f \$(docker images | grep sausage-backend | awk '{print \$3}') || true"
  #  rules:
  #     - when: manual
  #  dependencies:
  #     - vault
